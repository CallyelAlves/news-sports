// import { loadNews } from "./api";

// Inicializa a aplicação e carrega os componentes
document.addEventListener('DOMContentLoaded', () => {
    // Carrega o cabeçalho
    loadHeader();
  
    // Carrega as notícias
    // loadNews();
});
  